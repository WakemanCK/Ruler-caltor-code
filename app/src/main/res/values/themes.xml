<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Color1" parent="Theme.MaterialComponents">
        <!-- Calculator color. -->
        <item name="android:windowBackground">@color/white</item>
        <item name="colorPrimarySurface">@color/gray</item>
        <item name="colorPrimaryVariant">@color/grayLight</item>
        <!-- List color. -->
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorOnSecondary">@color/yellowDark</item>
        <!-- Screen color. -->
        <item name="cardForegroundColor">@color/white</item>
        <!-- Text color. -->
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="Theme.Color2" parent="Theme.MaterialComponents">
        <!-- Calculator color. -->
        <item name="android:windowBackground">@color/black</item>
        <item name="colorPrimarySurface">@color/gray</item>
        <item name="colorPrimaryVariant">@color/grayDark</item>
        <!-- List color. -->
        <item name="colorSecondary">@color/blueDark</item>
        <item name="colorOnSecondary">@color/grayLight</item>
        <!-- Screen color. -->
        <item name="cardForegroundColor">@color/black</item>
        <!-- Text color. -->
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="Theme.Color3" parent="Theme.MaterialComponents">
        <!-- Calculator color. -->
        <item name="android:windowBackground">@color/red</item>
        <item name="colorPrimarySurface">@color/redLight</item>
        <item name="colorPrimaryVariant">@color/redDark</item>
        <!-- List color. -->
        <item name="colorSecondary">@color/yellowDark</item>
        <item name="colorOnSecondary">@color/grayLight</item>
        <!-- Screen color. -->
        <item name="cardForegroundColor">@color/yellowDark</item>
        <!-- Text color. -->
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="Theme.Color4" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Calculator color. -->
        <item name="android:windowBackground">@color/yellow</item>
        <item name="colorPrimarySurface">@color/white</item>
        <item name="colorPrimaryVariant">@color/yellowDark</item>
        <!-- List color. -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorOnSecondary">@color/redLight</item>
        <!-- Screen color. -->
        <item name="cardForegroundColor">@color/white</item>
        <!-- Text color. -->
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="Theme.Color5" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Calculator color. -->
        <item name="android:windowBackground">@color/blue</item>
        <item name="colorPrimarySurface">@color/blueLight</item>
        <item name="colorPrimaryVariant">@color/blueDark</item>
        <!-- List color. -->
        <item name="colorSecondary">@color/gray</item>
        <item name="colorOnSecondary">@color/grayLight</item>
        <!-- Screen color. -->
        <item name="cardForegroundColor">@color/white</item>
        <!-- Text color. -->
        <item name="android:textColorPrimary">@color/black</item>
    </style>
</resources>